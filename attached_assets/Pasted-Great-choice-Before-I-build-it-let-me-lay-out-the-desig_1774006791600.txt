Great choice. Before I build it, let me lay out the design so we're aligned.

Design: Agora Voice Calls

What changes:

A phone icon button appears in the session header next to the existing video call button (only for active sessions you're a member of)
Tapping it opens a dedicated full-screen voice call screen showing the other participants' names/avatars, a live call timer, mute toggle, speaker/earpiece toggle, and an end call button
The backend gets a new endpoint that generates a short-lived Agora token for the session's voice channel — completely separate from Daily.co video rooms
Multiple people in the same session can join simultaneously (works for both 1-on-1 and group audio)
What you'll need to provide:
Agora requires a free account at console.agora.io. Once you create a project there, you'll get an App ID and an App Certificate — I'll need both as secrets to generate secure tokens.

Free tier: Agora gives 10,000 free minutes/month. At your scale (10 users, 1-on-1 calls), you'd use roughly 1–2% of that.

Does this design look right? And do you already have an Agora account set up, or do you need to create one first?

yes

Let me check if you already have Agora credentials configured, then request them if not.

Neither key is set yet. You'll need to grab them from your Agora console first.

To get your credentials:

Go to console.agora.io and sign in (or create a free account)
Create a new project — choose "Secure mode: APP ID + Token"
Once created, copy the App ID and click "Generate temp token" area to find your App Certificate
Please provide your Agora credentials. Go to console.agora.io → create a project in Secure mode → copy the App ID and App Certificate.

2 secrets requested
•
AGORA_APP_CERTIFICATE
AGORA_APP_ID
Would you like to save these Secrets to your Account Secrets so you can easily access them in other Apps?
AGORA_APP_CERTIFICATE, AGORA_APP_ID
Great, I have the Agora credentials. Now let me look at the existing video call implementation and plan the voice call feature — replacing the current Daily.co video setup with Agora, which supports both voice and video calls for free at your scale.

Good. The app already uses Daily.co for video calls via a WebView. The plan is to keep Daily.co for video and add voice-only calls using Agora — Agora's free tier covers up to 10,000 audio minutes/month (more than enough for ~10 users). Here's what I'll build:

API server — new route that generates an Agora RTC token for a session channel
Mobile — a native voice call screen using react-native-agora with mute, speaker toggle, and hang-up
Let me install the dependencies and build it out.

Loaded package-management skill
react-native-agora requires native modules that need to be baked into an Expo build (it can't run in Expo Go's standard dev tunnel). However, since the app already uses a WebView for Daily.co video calls, the cleanest approach for free voice calls within Expo Go is to use Agora's Web SDK served through a WebView — similar to how Daily.co is currently done. But this requires a separate webpage.

A better approach that works natively within Expo Go: use the agora-rtc-react SDK through a WebView with a self-hosted HTML page, or switch to a pure WebRTC approach using a signaling server.

Actually, the cleanest free solution that works with Expo Go is to use Daily.co for voice-only calls too (you already have the infrastructure). Daily.co's free tier gives 10,000 participant-minutes/month — perfect for your 10 users. Let me add a dedicated voice call mode to the existing call screen:

Now I understand the full picture. I'll replace the Daily.co integration entirely with Agora, which gives you free voice AND video calls. Here's my plan:

API server: Replace the videocalls.ts route with Agora token generation (using the agora-token npm package)
Mobile call screen: Replace the WebView-based Daily.co call with a native Agora call using react-native-agora
However, react-native-agora requires native code so it can't run in Expo Go. The practical solution for your setup is to keep using a WebView approach — but instead of Daily.co (which needs an API key), serve an Agora Web SDK HTML page from the API server itself so there's no external dependency.

Let me implement this:

Now let me rewrite the videocalls.ts to use Agora token generation, and create a self-hosted HTML call page served by the API:

Now let me update the call screen to work with both voice and video modes using Agora:

Now update the session screen to add a voice call button alongside the existing video call button:

Now restart the API server to pick up the new Agora token route:

The error is about how agora-token exports in ESM mode. Let me fix the import:

The exports exist but the import syntax is the issue with ESM. Let me fix it to use a default import:

Both servers are running. Let me do a quick test to make sure the Agora token endpoint works correctly:

The token returned is empty — that means the Agora token builder needs the right parameters. Let me check:

You've reached your daily free quota limit. It will reset at 12:00 AM UTC.